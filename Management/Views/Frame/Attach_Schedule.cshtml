@model DisplayMonkey.Models.ScheduleSelector

@{
    ViewBag.Title = Resources.DesignateSchedule;
}

<h2>@ViewBag.Title</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)
    @Html.Referrer()

    <fieldset>
        <legend>@Resources.Frame</legend>

        @Html.HiddenFor(m => m.FrameId)
        
        <div class="editor-label">
            @Html.LabelFor(s=>s.ScheduleId)
        </div>
        <div class="editor-field select">
            @Html.DropDownListFor(l => l.ScheduleId, ViewBag.Schedules as SelectList)
        </div>

        <p>
            <input type="submit" value="@Resources.Designate" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink(Resources.ViewFrames, "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
