@model DisplayMonkey.Models.Frame

<h2>@Resources.DesignatedLevels</h2>

@Html.ActionLink(Resources.DesignateLevel, "Attach", "Frame", new { id = Model.FrameId }, null)

@if (Model.Locations.Count == 0)
{
    <p>@Resources.ThisFrameWillShowInAllLocations</p>
}

else
{
    <table class="grid">
        
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Levels.FirstOrDefault().Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Levels.FirstOrDefault().Area)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model.Levels)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Area.Name)
            </td>
            <td>
                @Html.ActionLink(Resources.Remove, "Detach", "Frame", new { id = Model.FrameId, locationId = item.LevelId }, null)
            </td>
        </tr>
    }

    </table>
}